// Intentionally empty.  Older revisions rebuilt every Canvas after a domain
// reload and overwrote later layout work.  The final project installer is
// explicitly versioned and is the only tool allowed to update all scenes.
public static class ApplyPolishedRpgUiOnce { }
